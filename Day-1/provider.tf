# contains provider details
provider "aws" {
    # region     = "ap-south-1"
    # access_key = "my-access-key"
    # secret_key = "my-secret-key"
#   no need to provide any provoder details, as we have already configured aws cli
#   it will fetch details from .aws foler

}